name = "CAN Converter"
version = "0.1.0"
description = "Convert .blf / .asc / .log / .csv / .trc CAN logs to .trz in the browser"
author = "Zelos"
repository = "https://github.com/zeloscloud/zelos-app-can"
icon = "assets/icon.svg"
readme = "README.md"

[host]
type = "app"

[host.app]
kind = "web_app"
entry = "dist/index.html"

[package]
paths = ["dist"]
