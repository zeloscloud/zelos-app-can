# CAN Converter

Convert CAN logs to `.trz` — runs at [app.can.zeloscloud.io](https://app.can.zeloscloud.io).

- 📂 Drop a CAN log (`.blf` / `.asc` / `.log` / `.csv` / `.trc`)
- 📋 Drop a `.dbc`
- ▶️ Click **Convert**
- 💾 Get a `.trz`
